import { styled } from '../Styles'

export const HomeContainer = styled('main', {
  width: '100%',

  height: 'calc(100vh - 16.25rem)', //tamanho fixo

  backgroundColor: 'red',
  marginBottom: '8.5rem'
})

export const Items = styled('a', {
  width: 400,
  height: 500
})
